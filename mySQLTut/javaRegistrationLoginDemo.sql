create database registerandlogin;
use registerandlogin;
create table registration(
	name varchar(100),
    gender varchar(10),
    city varchar(50),
    email varchar(50),
    password varchar(50)
);
select * from registration;